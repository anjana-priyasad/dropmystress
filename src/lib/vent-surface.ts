/** Shared by the textarea and the burn animation so the swap between them is seamless. */
export const VENT_SURFACE_CLASS =
  "w-full min-h-[38vh] rounded-3xl border border-mist/12 bg-ink/[0.03] backdrop-blur-xl p-6 sm:p-8 text-lg leading-relaxed text-ink whitespace-pre-wrap break-words";
